<%= form_with(model: product, local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name, id: :product_name %>
  </div>

  <div class="field">
    <%= form.label :types %>
    <%= form.number_field :types, id: :product_types %>
  </div>

  <div class="field">
    <%= form.label :categories %>
    <%= form.number_field :categories, id: :product_categories %>
  </div>

  <div class="field">
    <%= form.label :sets %>
    <%= form.number_field :sets, id: :product_sets %>
  </div>

  <div class="field">
    <%= form.label :codes %>
    <%= form.text_area :codes, id: :product_codes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
